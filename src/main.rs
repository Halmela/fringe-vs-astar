use std::error::Error;
use std::fs;

fn main() {
    //read_map("".into()).unwrap();
}

/* fn read_map(file_path: String) -> Result<(Vec<bool>), Box<dyn Error>> {
    let contents = fs::read_to_string(file_path)?;

    println!("With text:\n{contents}");
    Ok(vec![])
} */

/* struct MapVec {
    map: Vec<bool>,
    height: u32,
    width: u32,
} */

/*
.map -> Map -> Graph -> Pathfinder -> Presenter
.scen -> [Scenario] -->^
*/
